namespace Lighthouse.Scene
{
    /// <summary>
    /// Indicates the direction of a scene transition: Forward when navigating to a new scene, Back when returning.
    /// </summary>
    public enum TransitionDirectionType
    {
        Forward,
        Back,
    }
}